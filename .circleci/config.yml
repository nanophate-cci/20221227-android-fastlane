version: 2.1

jobs:
  build:
    docker:
      - image: cimg/android:2023.02
    steps:
      - run: ruby -v
      - run: sudo apt-get update && sudo apt-get install -y ruby-full
      - run: ruby -v
